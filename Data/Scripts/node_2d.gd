extends Node2D

# This script appears to be a template or unused code for a scrolling background.
# The code below is commented out.

#@export var speed := 500.0
#@onready var bg1: Sprite2D = $BG1
#
#
#@onready var bg2: Sprite2D = $BG2
#
#
#var width := 0.0
#
#func _ready():
 	#width = bg1.texture.get_width() * bg1.scale.x - 143
	#
#
#
#func _process(delta):
	#bg1.position.x -= speed * delta
	#bg2.position.x -= speed * delta
#
	#if bg1.position.x <= -width:
		#bg1.position.x = bg2.position.x + width
#
	#if bg2.position.x <= -width:
		#bg2.position.x = bg1.position.x + width

